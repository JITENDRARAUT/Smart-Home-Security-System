sudo su
apt update
python3 --version
apt install python3-pip python3-venv
apt install python3-pip python3-venv
cd fastapi-lab
python3 -m venv venv
source venv/bin/activate
pip3 install fastapi uvicorn tinydb jinja
nano app.py
uvicorn app:app --host 0.0.0.0 --port 80
